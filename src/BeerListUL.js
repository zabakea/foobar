// import { useState } from "react";

export default function BeerListUL({ data }) {
    // const initColor = "";

    // const [color, setColor] = useState(initColor);

    // const buttonClicked = event => {
    //     setColor(Number(event.target.value));

    // };
    // console.log(color);

    console.log(data);
    // const beername = Object.keys(props).map(key => (<li style={{ color: color }} onClick={() => { setColor("red") }} key={props[key].id}>{props[key].beer}</li>));

    // const beername = Object.keys(props).map(key => (<li key={props[key].id}>{props[key].beer}</li>));

    return (
        <ul>

        </ul>
    )
}
